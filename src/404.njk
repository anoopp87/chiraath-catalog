---
permalink: 404.html
---
{% set whatsapp_main = "919567883837" %}

<style>
  :root{--text:#111;--muted:rgba(0,0,0,.65);--border:#eee;--border2:#ddd}
  *{box-sizing:border-box}
  body{
    margin:0;
    font-family:system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;
    background:linear-gradient(180deg,#faf7f2 0%,#ffffff 55%,#fbfbfb 100%);
    color:var(--text);
  }
  .wrap{
    max-width:680px;
    margin:40px auto;
    padding:32px 20px;
    background:rgba(255,255,255,0.85);
    border:1px solid rgba(0,0,0,0.06);
    border-radius:22px;
    box-shadow:0 12px 30px rgba(0,0,0,0.06);
    text-align:center;
  }
  .emoji{font-size:3.5rem;margin-bottom:12px}
  h1{font-size:22px;margin:0 0 10px}
  p{color:var(--muted);font-size:14px;line-height:1.6;margin:0 0 24px}
  .btn{
    display:inline-block;
    padding:12px 28px;
    border:1px solid var(--border2);
    border-radius:14px;
    text-decoration:none;
    color:var(--text);
    font-weight:800;
    background:#fff;
    font-size:14px;
  }
  .btn.primary{background:#111;color:#fff;border-color:#111}
  .actions{display:flex;gap:10px;justify-content:center;flex-wrap:wrap}
</style>

<div class="wrap">
  <div class="emoji">🙈</div>
  <h1>This page doesn't exist</h1>
  <p>The piece you're looking for may have sold out or moved.<br>Browse our latest collection below.</p>
  <div class="actions">
    <a class="btn primary" href="/">Browse Catalog</a>
    <a class="btn" href="https://wa.me/{{ whatsapp_main }}" target="_blank" rel="noopener">WhatsApp Us</a>
  </div>

  <div style="margin-top:32px">
    {% include "black-footer.njk" %}
  </div>
</div>
